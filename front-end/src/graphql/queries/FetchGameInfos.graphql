query FetchGameInfos($gameId: String!) {
  fetchGameInfos(gameId: $gameId) {
    id
    user1 {
      id
      nickname
    }
    user2 {
      id
      nickname
    }
    whoWon
    whoseMove
    gameBoard
    messages
    gameUUID
  }
}
