mutation CreateGame($googleCaptchaToken: String!) {
  createGame(googleCaptchaToken: $googleCaptchaToken) {
    id
    user1 {
      id
      nickname
    }
    user2 {
      id
      nickname
    }
    gameBoard
    whoseMove
        gameUUID
  }
}

